// Generated by CoffeeScript 1.6.3
(function() {
  var DashingClient, exports, lodash, request;

  lodash = require('lodash');

  request = require('request');

  DashingClient = (function() {
    function DashingClient(dashingBaseUri, apiKey, options) {
      this.dashingBaseUri = dashingBaseUri;
      this.apiKey = apiKey;
      this.options = options;
    }

    DashingClient.prototype.send = function(widgetId, data, done) {
      var options;
      data = lodash.extend({}, data, {
        auth_token: this.apiKey
      });
      options = lodash.extend({
        json: data
      }, this.options);
      return request.post("" + this.dashingBaseUri + "/widgets/" + widgetId, options, done);
    };

    DashingClient.prototype.toString = function() {
      return "DashingClient for " + this.dashingBaseUri;
    };

    return DashingClient;

  })();

  module.exports = exports = DashingClient;

}).call(this);
